/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Lease} from './lease';

describe('Lease', () => {
  it('should create an instance', () => {
    expect(new Lease()).toBeTruthy();
  });
});
